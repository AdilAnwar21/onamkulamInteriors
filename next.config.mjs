/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/onamkulamInteriors',
    output: 'export',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
